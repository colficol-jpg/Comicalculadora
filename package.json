{
  "name": "comicalculadora",
  "version": "1.0.0",
  "description": "Simulador de crédito ComiOnline con Tailwind y Express",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "npx tailwindcss -i ./input.css -o ./public/styles.css --minify",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}
